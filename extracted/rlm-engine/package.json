{
  "name": "rlm-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Recursive code-writing LLM agents over a persistent shared world, with LMSR-priced claims. Extracted from the Marketplace of Ideas experiment (2026-06).",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ax-llm/ax": "^22.0.3",
    "better-sqlite3": "^12.10.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.8"
  }
}
